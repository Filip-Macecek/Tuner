{
  "name": "tuner",
  "version": "0.1.0",
  "description": "Guitar tuner written in plain JS with no dependencies.",
  "main": "dist/script.js",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "tsc && vite build && vite preview"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "vite": "^6.3.2",
    "typescript": "^5.8.3"
  }
}
